<div class="form-group">
            <!--<input type="text" class="form-control" ng-model="clientid" placeholder="Component Type" required=""/>-->
      <select ng-model="selectedName" ng-options="x for x in names" class="form-control" ng-change="fetch()">
      
      </select>
      <BR/><BR/>
      <table class="table">
      <tr>
         <th class="active"><b>Serial</b></th>
         <th class="active"><b>Name</b></th>
         <th class="active"><b>Description</b></th>
         <th class="active"><b>Select</b></th>
         
         
      </tr>
        <tr ng-repeat="x in Campaigns">
        <td>{{ $index + 1 }}</td>
        <td>{{ x.name }}</td>
        <td>{{ x.description }}</td>
        <td><input type="checkbox" ng-model="x.selected"></td>
        </tr>
      </table>
      
            <button class="btn btn-success pull-right" ng-click="createbody()">Proceed</button>
            <span class="text-danger">{{ error }}</span>
      
</div> 